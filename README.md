# Tiny-OS
Tiny-OS is a minimal operating system that aims to run on multiple embedded platforms, including Raspberry Pi and stm32.

## Getting started
Download arm developer toolchain from https://developer.arm.com/downloads/-/arm-gnu-toolchain-downloads. 
Make necessary changes to `GCC` variable in the Makefile and hit `make` in terminal.